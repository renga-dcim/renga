[workspace]
members = ["cargo:."]
packages = ["renga-agent"]

[dist]
cargo-dist-version = "0.32.0"
ci = []
targets = [
    "x86_64-unknown-linux-musl",
    "aarch64-unknown-linux-musl",
]
unix-archive = ".tar.gz"
checksum = "sha256"
