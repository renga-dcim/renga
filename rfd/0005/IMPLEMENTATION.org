#+TITLE: RFD 0005 implementation checklist

Implements [[file:README.adoc][RFD 5: Physical containment]].

- [X] Add resource envelopes and typed projections for site groups, sites,
  nested locations, and racks.
- [X] Enforce organization and same-site containment with composite foreign keys
  and reject recursive hierarchy cycles.
- [X] Add rack geometry, bounded dimensions, unit convention, and transactional
  safe-resize validation under the same lock as current placement.
- [X] Add separate desired and reconciled current placement per physical
  resource with site/location/rack consistency and support for unplaced,
  freestanding, and zero-U equipment.
- [X] Add face-specific PostgreSQL range occupancy with overlap exclusion and
  transactional current placement, move, resize, and removal operations; desired
  moves produce feasibility findings without reserving occupancy.
- [X] Add observation-linked placement evidence, source mapping, confidence,
  complete-snapshot-aware staleness, and ambiguity/conflict findings without
  overwriting desired placement.
- [X] Add authenticated organization-scoped site, location, rack elevation,
  unplaced-resource, and placement-finding LiveViews in the existing
  `live_session :require_authenticated_user` router block.
- [X] Add tenant, inverse-parent cycle concurrency, occupancy and geometry race,
  evidence, reconciliation, authorization, and LiveView tests.
- [X] Run `make check-rfds`, focused DCIM tests, and `mix precommit` before marking
  the proposal implemented.
